# PostgreSQL — Docker Compose (port 5433 to avoid conflict with existing local PG)
DATABASE_URL="postgresql://pecker:pecker_dev@localhost:5433/pecker_chess"

# Next.js public base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
